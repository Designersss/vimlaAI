import {
  DEFAULT_VIMLA_LOCALE,
  VIMLA_LOCALES,
  type VimlaLocale,
} from "@vimla/shared";

export const locales = VIMLA_LOCALES;
export const defaultLocale = DEFAULT_VIMLA_LOCALE;

export type AppLocale = VimlaLocale;
