{
  "name": "@vimla/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "prisma"],
  "scripts": {
    "generate": "prisma generate --schema ./prisma",
    "prepare": "prisma generate --schema ./prisma",
    "build": "prisma generate --schema ./prisma && tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "ensure-test-db": "tsx scripts/ensure-test-database.ts",
    "migrate:dev": "prisma migrate dev --schema ./prisma",
    "migrate:deploy": "prisma migrate deploy --schema ./prisma",
    "studio": "prisma studio --schema ./prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "dotenv": "^16.6.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "eslint": "^9.35.0",
    "prisma": "^6.15.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
